// Update all documents where name = "Holberton school"
db.school.update(
    { name: "Holberton school" },               // Filter: select matching documents
    { $set: { address: "972 Mission street" } },// Add new field "address"
    { multi: true }                             // Ensure ALL matching documents are updated
);
